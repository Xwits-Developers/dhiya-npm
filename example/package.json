{
  "name": "dhiya-chatbot-example",
  "version": "1.0.0",
  "description": "Example chatbot application using dhiya-npm",
  "type": "module",
  "scripts": {
    "dev": "vite --force",
    "build": "vite build",
    "preview": "vite preview",
    "install:clean": "rm -rf node_modules package-lock.json && npm install --omit=optional"
  },
  "dependencies": {
    "dhiya-npm": "file:..",
    "@xenova/transformers": "^2.17.2",
    "idb": "^8.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vite": "^5.4.11"
  }
}
